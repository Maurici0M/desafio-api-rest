1 -> N (INDIRETO)

CREATE TABLE X (
ID INT PRIMARY KEY,
NOME VARCHAR(50));

CREATE TABLE Y (
 ID INT PRIMARY KEY,
 ID_X INT,
 CONSTRAINT fk_y_x
 FOREIGN KEY (ID_X)
 REFERENCES X (ID));

y1 -> x1
y2 -> x1

----
1 -> N (DIRETO)

CREATE TABLE X (
ID INT PRIMARY KEY,
NOME VARCHAR(50));

CREATE TABLE Y (
  ID INT,
  ID_X INT,
 PRIMARY KEY (ID, ID_X),
 CONSTRAINT fk_y_x
 FOREIGN KEY (ID_X)
 REFERENCES X (ID));

---
1 -> N (INDIRETO)

CREATE TABLE X (
ID INT PRIMARY KEY,
NOME VARCHAR(50));

CREATE TABLE Y (
 ID INT PRIMARY KEY,
 NOME VARCHAR(50));

CREATE TABLE X_Y (
  ID INT PRIMARY_KEY
  ID_X INT,
  ID_Y INT,
CONSTRAINT fk_x
 FOREIGN KEY (ID_X)
 REFERENCES X (ID),
 CONSTRAINT fk_y
 FOREIGN KEY (ID_Y)
 REFERENCES Y (ID));

x_y 1 -> x1, y1
x_y 2 -> x1, y2

----
1 -> 1

CREATE TABLE X (
ID INT PRIMARY KEY,
NOME VARCHAR(50));

CREATE TABLE Y (
 ID_X INT PRIMARY KEY,
 CONSTRAINT fk_y_x
 FOREIGN KEY (ID_X)
 REFERENCES X (ID));

x1 -> y1
x2 -> y2

----
N -> N

CREATE TABLE X (
ID INT PRIMARY KEY,
NOME VARCHAR(50));

CREATE TABLE Y (
ID INT PRIMARY KEY,
NOME VARCHAR(50));

CREATE TABLE X_Y (
ID_X INT,
ID_Y INT,
PRIMARY KEY (ID_X, ID_Y),
CONSTRAINT fk_x
 FOREIGN KEY (ID_X)
 REFERENCES X (ID),
 CONSTRAINT fk_y
 FOREIGN KEY (ID_Y)
 REFERENCES Y (ID));

x1, y1
x2, y1
x1, y2
x2, y2
